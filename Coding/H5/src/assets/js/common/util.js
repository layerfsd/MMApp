const Util = {
	/*验证手机号*/
	checkPhone(value){
		let regExp = /,,,/;
		if(!regExp.test(value)){
			/*给出相应的错误提示*/
		}
	},

	/*验证密码*/
	checkPassword(value){
		let regExp = /,,,/;
		if(!regExp.test(value)){
			/*给出相应的错误提示*/
		}
	},

	/*验证个人信息*/
};

export default Util;